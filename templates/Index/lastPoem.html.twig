<ul>
	{% for entity in entities %}
		<li>
			<a href="{{ path('read', {'id' : entity.id, 'slug' : entity.slug }) }}" >{{ entity.title }}</a>
			<br>
			{% if entity.biography is not empty %}
				<i class="fa fa-chevron-circle-right"></i> 
				<span class="bold">{{ entity.biography.title }}</span>
			{% endif %}
		</li>
	{% endfor %}
</ul>